---
- name: Install Steam
  hosts: all
  become: true
  connection: local
  tasks:
    - name: Install steam related packages
      kewlfft.aur.aur:
        name:
          - steam
          - game-devices-udev
          - gamemode
